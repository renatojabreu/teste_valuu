import 'package:flutter/material.dart';

class Premios extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Prêmios")),
    );
  }
}
